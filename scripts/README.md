# Scripts

Helper scripts for packaging and release automation.

## Testing

- `scripts/testing/verify-critical-coverage.ps1`: validates critical-surface coverage thresholds and required scenario tests declared in `docs/30-contracts/test-gate.contract.yaml`.
